function reversePrint(input) {
    let n = Number(input[0]);
    let numbers = ""
    for(let i = n; i >= 1; i-- ){
        numbers = numbers + i + ",";
console.log(i);
    }
    
    }
    reversePrint (["10"])